<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TouchalyticsApp"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.TouchalyticsApp"/>
        <!-- HUB is the ONLY launcher -->
        <activity
            android:name=".MainMenuActivity"
            android:exported="true"
            android:label="App Hub">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Wordle (no launcher filter) -->
        <activity
            android:name=".WordleActivity"
            android:label="Wordle"
            android:parentActivityName=".MainMenuActivity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:launchMode="singleTop" />

        <!-- News / Media -->
        <activity
            android:name=".NewsMediaActivity"
            android:label="News / Media"
            android:parentActivityName=".MainMenuActivity" />

        <!-- Fruit Ninja -->
        <activity
            android:name=".FruitNinjaActivity"
            android:label="Fruit Ninja"
            android:parentActivityName=".MainMenuActivity" />

    </application>

</manifest>